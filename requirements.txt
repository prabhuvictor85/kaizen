pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0
scipy>=1.10.0
# PINNED, not >=: yfinance 1.5.x leaks file descriptors across a long
# sequential run (~1600 tickers in one process), degrading into cascading
# "unable to open database file" failures and finally OSError [Errno 24]
# Too many open files. 1.3.0 is the version this pipeline was validated
# against. See requirements-lock.txt for the full validated set.
yfinance==1.3.0
pandas-market-calendars>=4.3.0
requests>=2.31.0
lxml>=4.9.0
lightgbm>=4.0.0
catboost>=1.2.0
xgboost>=2.0.0
scikit-learn>=1.3.0
optuna>=3.3.0
shap>=0.43.0
matplotlib>=3.7.0
